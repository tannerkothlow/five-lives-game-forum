const router = require('express').Router();
const { User } = require('../models');

// GET '/'
// render login

// POST '/'
// checks the entered password and either rejects if doesnt match or redirects to home and logs in

module.exports = router;